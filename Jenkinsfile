pipeline {
    agent any
    stages {

        stage('Send Dockerfile to docker server') {
            steps {
		echo "Welcome"
		sh 'ls -ltr'
		sh 'pwd'
            }
        }
    }
}


